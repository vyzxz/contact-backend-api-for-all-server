NODE_ENV=production
PORT=3001

# Zoho Email
EMAIL_SERVICE=Zoho
SMTP_HOST=smtp.zoho.com # this is example if u use google so smtp.google.com , this
SMTP_PORT=587 # For Tls and if u have ssl so use 465
EMAIL_USER=
EMAIL_PASS= # app password if u use gmail and host like zoho
EMAIL_FROM=
EMAIL_TO=
ADMIN_NAME=

# CORS - Update with your domain
CORS_ORIGIN=https://yourdomain.com

# Optional: Monitoring
LOG_LEVEL=info